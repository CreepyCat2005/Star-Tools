<UserControl x:Class="Star_Citizen_Pfusch.Models.UserControls.ShipListDisplayItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Star_Citizen_Pfusch.Models.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="450" MinWidth="150" >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Border CornerRadius="20" SizeChanged="Border_SizeChanged" Grid.RowSpan="2" >
            <Border.Background>
                <LinearGradientBrush>
                    <GradientStop Offset="0" >
                        <GradientStop.Color>
                            <Color A="255" R="55" G="55" B="55" />
                        </GradientStop.Color>
                    </GradientStop>
                    <GradientStop Offset="1" >
                        <GradientStop.Color>
                            <Color A="255" R="70" G="70" B="70" />
                        </GradientStop.Color>
                    </GradientStop>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <Border CornerRadius="20" SizeChanged="Border_SizeChanged" Margin="10,10,10,8" >
            <Border.Background>
                <ImageBrush ImageSource="{Binding ImageSource}" Stretch="UniformToFill" />
            </Border.Background>
        </Border>

        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Viewbox Margin="8,0,8,8" >
                <TextBlock Text="{Binding ManufacturerName}" Foreground="{DynamicResource TextColor}" ToolTip="Manufacturer" />
            </Viewbox>
            <Viewbox Margin="8,0,8,8" Grid.Row="1" >
                <TextBlock Text="{Binding Cargo}" Foreground="{DynamicResource TextColor}" ToolTip="Cargo" />
            </Viewbox>
            <Viewbox Margin="8,0,8,8" Grid.Row="1" Grid.Column="1" >
                <TextBlock Text="{Binding Weight}" Foreground="{DynamicResource TextColor}" ToolTip="Weight" />
            </Viewbox>
            <Viewbox Margin="8,0,8,8" Grid.Column="1" >
                <TextBlock Text="{Binding ShipName}" Foreground="{DynamicResource TextColor}" ToolTip="Name" />
            </Viewbox>

            <Rectangle VerticalAlignment="Top" Grid.ColumnSpan="2" Height="1" >
                <Rectangle.Fill>
                    <LinearGradientBrush>
                        <GradientStop Color="Transparent" Offset="0" />
                        <GradientStop Color="Gray" Offset=".5" />
                        <GradientStop Color="Transparent" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

        </Grid>
    </Grid>
</UserControl>
